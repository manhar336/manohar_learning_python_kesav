"""
*******************Method | Functions**************************

re.match()  --This function attempts to match RE pattern to String with optional flag
Syntax: re.match(pattern,string,flags)  #pattern means what we are searching ,string means where we are searching
and pattern is substring ,string is string and variable and flags
pattern : Regular expression to be matched
string : which would be searched to match the pattern at the beginning of the string
Flags : We can specify different flags using bitwise OR (|)
We use group(num) or groups(num) function of match object to get matched function
group(num=0) : Returns entire match(or specific sub group num)
groups(): Returns all matching subgroups in a tuple output

pip search regex
pip download regex
pip install regex
"""
